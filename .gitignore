# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Secrets
.env
!.env.example

# Cached orbital data (regenerated on demand)
backend/data_cache/*.cache
backend/data_cache/*.meta.json

# Node
node_modules/
dist/
.vite/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# PDF build tooling (installed on demand, not a runtime dependency)

# Landing-page design prototype.
# Superseded by the shipped implementation in frontend/src/components/landing.
# Kept on disk as the approved design reference; not committed because its
# preview/ folder is ~15 MB of screenshots and video of a now-outdated draft.
kaksha-landing-preview/
